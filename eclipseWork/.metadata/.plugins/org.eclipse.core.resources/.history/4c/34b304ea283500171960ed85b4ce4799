package mdtrmGuzy;

import static org.junit.Assert.*;
import org.junit.Test;

public class PersonTEST {
	/*@Test(expected=Error.class)
	public void personConstructorRejectsInvalidTitle(){
		Person person1 = new Person("Larry", "ThePlug", 500000);
		Person person2 = new Person("Fernando", "Worker", 10.50);
		if (person1.toString()=="ThePlug")
			throw new IllegalArgumentException();
		assertFalse("toString does not work properly", person2.toString() == "Worker");
	}*/

}
